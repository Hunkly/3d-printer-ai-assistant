# OpenRouter free selector

Narrow, inference-free selector for exact currently verified-zero-cost OpenRouter models and Codex compatibility evidence. It never chooses paid or generic router aliases and never writes the user-maintained compatibility registry.
