"""MCP tool groups.

Tools are plain functions in this package, registered on the server in
``print_engineer.mcp.server`` under a ``group.name`` namespace
(``system.*``, ``printer.*`` now; ``slicer.*``, ``analysis.*``,
``history.*`` in later phases).
"""
